<?xml version="1.0" encoding="utf-8"?>
<!--
    packageNames is set deliberately: the system delivers events ONLY
    for these apps, which saves meaningful battery and is the first of
    two limits on what can be read.

    It is not sufficient on its own. An event names the app that
    produced it, not the window on top, so rootInActiveWindow can
    return the task switcher while a feed event is being handled. The
    service therefore also verifies the active window's package before
    reading it. Both limits together are what a consent form should
    describe.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowContentChanged|typeWindowStateChanged|typeViewScrolled"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="500"
    android:packageNames="com.ss.android.ugc.aweme,com.ss.android.ugc.aweme.lite,com.zhiliaoapp.musically,com.tiktok.lite.go" />
